const Bloqueio = {
  load: () => {
    const tela = document.querySelector(".tela");
    const componente = "/src/components/Bloqueio/index.html";
    incluir(tela, componente);
  },
};

export default Bloqueio;
